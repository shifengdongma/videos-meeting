接下来我们将对当前的前端项目进行全面的视觉重构。
1. 请先读取并学习 GitHub 仓库 `nextlevelbuilder/ui-ux-pro-max-skill` 中的核心设计准则（如果你无法直接联网，请遵循：极致的留白比例、Vercel 风格的极简色彩系统、恰到好处的毛玻璃阴影、严格的 Tailwind 间距规范、丝滑的 Framer Motion 微交互）。
2. 从现在起，你将作为 "UI UX Pro Max" 首席设计工程师。
3. 请全面扫描我项目的 `src` 目录结构。我们需要统一全局样式，包括：
   - 侧边栏（Sidebar）：当前偏黑蓝色，需要更具质感的暗色主题或毛玻璃效果。
   - 统一样式变量（Design Tokens）：在 Tailwind/CSS 中定义好主色调（Primary Blue）、背景灰（Background Gray）和文字层级（Text Primary/Secondary）。
请不要修改任何业务逻辑，仅梳理和分析目录结构，回复“准备就绪”并给出你的全局 CSS/Tailwind 变量重构建议。


第一步：重构“登录和注册页面”

确认全局配置后，输入以下 Prompt：

现在请重构「登录页面」的相关文件（请自动寻找类似 Login.tsx / Login.vue 的文件）。
应用 UI UX Pro Max 准则，进行以下视觉升级：
1. 背景优化：使用更柔和的 Mesh Gradient（网格渐变）或带有磨砂玻璃质感的现代背景。
2. 左侧排版优化：拉开主标题“视频会议管理系统”与副标题的层级，使用更现代的无衬线字体（如 Inter），增加行高，并给特色列表（稳定的会议预约...）添加精致的左侧 Icon 或透明度卡片包裹。
3. 登录表单卡片优化：
   - 增加更柔和多层的 box-shadow (例如 shadow-xl 配合 shadow-blue-500/10)。
   - 输入框在 focus 时，添加丝滑的 ring 动画和边框颜色过渡。
   - “登录并进入系统”按钮使用现代的渐变色或带光泽感的纯色，悬浮时稍微上浮并加深阴影。
请直接修改代码文件并保存，保持所有原有的登录请求逻辑不变。

第二步：重构“控制台布局”与“数据看板”

等待登录页完成并确认没问题后，输入以下 Prompt：

接下来重构「全局控制台 Layout」以及「会议中心/直播中心」的页面。
应用 UI UX Pro Max 准则，重点优化以下区域：
1. 侧边栏与头部（Layout）：侧边栏菜单项在 Hover 和 Active 状态下，添加柔和的背景色和流畅的宽度动画。右上角的用户信息区（田曦薇）重构为更紧凑的 Avatar 组件。
2. 统计数据卡片（Stat Cards）：
   - 增大内部留白（Padding），数值（如 1、0）的字体加粗并变大，标签文字（全部会议）颜色减淡以突出主次。
   - 状态标签（如“实时进行”、“在线可用”）使用更精致的 Badge 样式（带有极其轻微的背景色和相配的文字颜色，如 Tailwind 的 bg-green-50 text-green-700）。
3. 数据表格（Table）：
   - 移除表格生硬的边框，采用无边框设计，仅保留柔和的底部分割线（border-b border-gray-100）。
   - 表头文本变小、加粗并使用 uppercase 风格。
   - 为每一行添加悬浮态（hover:bg-gray-50），“进入会议室”按钮改为幽灵按钮（Ghost button）或轻量描边按钮。
4. 空状态（Empty State）：图3的无数据插图过大且单调，请优化无数据时的展示，添加居中的灰色提示文字和柔和的引导按钮。
请修改相关组件文件并保存，保持业务逻辑不变。

第三步：重构高复杂度的“会议室控制台”

输入以下 Prompt：

最后，重构最具挑战的「会议控制台」页面（包含视频播放、桌面共享和投票模块的文件）。
运用 UI UX Pro Max 的空间收纳与卡片布局原则：
1. 模块化卡片：将“本地画面”、“桌面共享”等黑色播放区域使用圆角（rounded-2xl）包裹，增加统一的深色磨砂质感背景，未开启设备时的提示文字居中并辅以优雅的线性图标（Lucide/Heroicons）。
2. 控制栏布局优化：右上角的“打开摄像头”、“桌面共享”按钮，重构为悬浮控制条（Floating Control Bar）或更具现代感的按键，使用一致的高度和圆角，主次操作通过颜色（Primary vs Default）严格区分。
3. 侧边投票模块优化：
   - 这是一个重要信息区，将其背景调整为极其微弱的浅色（如 bg-slate-50），使其与主视频区域区分开。
   - 投票进度条（反对、弃权、赞成）改为更细、更现代的 Progress Bar 样式，两端圆角，并为进度条的加载添加缓动动画（transition-all duration-500）。数字百分比与标签水平对齐对齐。
请在修改文件时特别注意保持视频流、组件状态和 WebRTC 相关逻辑的完整性。

第四步 重构用户管理页面
在终端中输入以下 Prompt：

Plaintext
现在请专注于重构「管理后台 - 用户管理页面」的文件（请自动寻找类似 AdminUserManagement.tsx / AdminUserManagement.vue 的文件）。
这是管理后台的核心部分，涉及敏感的权限更改，因此需要极其严谨且专业的视觉和交互体验。应用 UI UX Pro Max 准则，进行以下全面的重构：

1. 统计卡片 (Stat Cards) 的专业度：
   - 移除原有的生硬卡片，将其重构为带有轻微磨砂玻璃效果的卡片，应用多层柔和 shadow（如 shadow-xl 配合 shadow-blue-500/5）。
   - 增大 Padding，数值（3、1、0、2）加粗变大并使用更具专业感的字体，标签（用户总数）变小、Uppercase 风格并降低透明度。
   - 增加卡片内部的空间感，确保数值和标签在视觉上更平衡。

2. 用户表格 (User Table) & 角色调整交互：
   - 移除表格生硬的边框和背景，采用无边框设计，仅保留柔和的底部分割线（border-b border-gray-100）。
   - 表头文本变小、加粗并使用 uppercase 风格。
   - 为每一行添加悬浮态（hover:bg-slate-50）。
   - **重写角色标签 (Badge)：** 目前的标签（绿、红、绿）太过于普通。重构为更具语义化的 Badge 样式。例如，管理员角色用更庄重的颜色（如深紫/深红）配合Icon（Lucide/Heroicons），普通用户用中性色（如浅灰/浅蓝）。
   - **重构角色调整下拉菜单：** 这是关键。移除原生 select，重构为自定义 Dropdown 组件，支持下拉动画、选项内 Icon/描述，并确保 Hover 和 Focus 态丝滑。在选项更改时，添加临时的“已保存”状态指示器或更清晰的确认/保存按钮，使其更像一个专业的管理系统。

3. 全局组件统一：
   - 应用与控制台 Prompt 相同的 Avatar 组件、退出按钮和 Active 态优化。确保“田曦薇 (系统管理员)”和“ADM 管理后台”的样式统一。

4. 空间与留白：
   - 确保整体布局整洁，统计卡片和表格之间、表格行之间都有足够的呼吸感（遵循 Tailwind 2xl spacing）。

请修改相关组件文件并保存，保持所有原有的后端数据拉取和权限更改请求逻辑不变。

第五步 重构会议室模板页面
现在请专注于重构「管理后台 - 会议室模板页面」的文件（请自动寻找类似 AdminRoomTemplates.tsx / AdminRoomTemplates.vue 的文件）。
这个页面目前处于“规划中”的空状态。根据 UI UX Pro Max 的“Empty State 设计美学”，我们需要把它做得极具期待感和高级感。请进行以下视觉重构：

1. 顶部信息卡片 (Info Cards) 质感升级：
   - 将“规划中”和“多场景”这两个卡片重构为具有 Vercel 风格的精致卡片。
   - 增加非常轻微的渐变背景（例如 bg-gradient-to-br from-white to-slate-50）和精细的边框（border border-slate-100）。
   - 主标题文字（如“规划中”）可以尝试加入细微的渐变色（bg-clip-text text-transparent bg-gradient-to-r from-slate-800 to-slate-500），提升精致度。
   - 增加卡片 hover 时的轻微上浮动画（-translate-y-1）和阴影加深效果。

2. 核心区空状态 (Empty State) 沉浸感重构：
   - 移除目前略显廉价和巨大的盒子插图。
   - 将底部的白色大卡片内部改为“虚线边框”或带有极浅底色的区块（例如 bg-slate-50/50 配合 border-2 border-dashed border-slate-200），营造出“插槽/待填充”的设计隐喻。
   - 在中央区域，使用高级的线性图标（推荐 Lucide-react 中的 LayoutTemplate, Sparkles 或 Blocks 图标，设置大尺寸和浅灰色/主色调透明度 text-blue-500/20）。
   - 优化提示文字：“后续可扩展为特殊会议室模板分配能力”——使用更现代的排版，主标题加粗（如“专属模板即将上线”），副标题使用 text-slate-500 并增加行高。
   - **点睛之笔：** 在文字下方添加一个处于 disabled 状态或幽灵按钮（Ghost Button）样式的“+ 创建新模板”按钮。即使目前不可点，也能极大地完善界面的功能暗示，让用户知道未来这里可以做什么。

3. 布局与间距对齐：
   - 确保顶部的卡片与底部的空状态容器之间的间距（Gap）与系统中其他页面的标准一致（如 gap-6 或 gap-8）。

请直接修改代码文件并保存，注意保持侧边栏和头部导航的全局一致性。